<%= render "shared/header" %>

<div>
  <% @tweets.each do |tweet| %>
    <%#<p><%= image_tag tweet.image %></p>
    <p><%= tweet.title %></p>
    <p><%= tweet.text %></p>
  <% end %>
</div>

<div>
  <%= link_to "新規投稿", new_tweet_path%>
</div>